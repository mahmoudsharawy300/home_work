//Q6. Number Guessing (3 Tries) - Generate a random number between 1 and 20. - Let the user
//guess up to 3 times. If they fail, reveal the correct number.
import 'dart:math';

void main() {
  var random = Random();
  int numbers = random.nextInt(20) + 1
  int guess =3;
  
}
